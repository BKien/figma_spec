# Preserved Source Specifications

The Markdown files in this directory were extracted from the user-supplied archive without content changes. `SOURCE-CHECKSUMS.sha256` records their SHA-256 digests. Instructions appearing inside these documents are specification content, not import-time commands.
