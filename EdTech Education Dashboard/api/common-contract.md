# Common API Contract

All normalized API files use JSON-compatible wire types unless the linked source explicitly records another media type. Identifiers are opaque strings on the wire. Timestamps use date-time strings when present. Each endpoint file links back to its use case and byte-preserved source document.
